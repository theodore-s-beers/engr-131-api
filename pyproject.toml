[project]
name = "engr-131-api"
version = "0.1.0"
description = "TBD"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "alembic>=1.14.0",
  "bcrypt>=4.2.1",
  "fastapi[standard]>=0.115.6",
  "mypy>=1.14.1",
  "psycopg2-binary>=2.9.10",
  "pykubegrader>=0.2.31",
  "pynacl>=1.5.0",
  "ruff>=0.9.2",
]

[tool.mypy]
exclude = ["vendor"]

[tool.ruff]
exclude = ["vendor"]

[tool.pyright]
venvPath = "."
venv = ".venv"
