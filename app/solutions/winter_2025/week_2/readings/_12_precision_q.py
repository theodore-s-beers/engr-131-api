from typing import Any

total_points: float = [2.0, 1.0, 3.0]

solutions: dict[str, Any] = {
    "q1-1-Floating-Point-Precision": 'abs((0.1 + 0.2) - 0.3) < 1e-10',
    "q3-1-Precision-Loss-tf": 'True',
    "q2-1-Number-Representation": ['Integers in Python have unlimited precision', 'Floating-point numbers can represent decimal numbers', 'The math.isclose() function is better than == for comparing floats'],
}
