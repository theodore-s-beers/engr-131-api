from typing import Any

total_points: float = [1.0, 1.0, 1.0, 2.0, 2.0]

solutions: dict[str, Any] = {
    "q1-1-Descriptive-Variable-Names": 'To make the code easier to understand and maintain',
    "q1-2-Converting-Variable-Types": 'Using the `float()` function to convert a string to a numeric type',
    "q3-1-Constants-tf": 'False',
    "q3-1-Rules-for-Naming-Variables": ['Variables must start with a letter or an underscore (`_`).', 'Variable names are case-sensitive.', 'Variables can include numbers but cannot start with them.'],
    "q3-2-Multi-Variable-Assignment": ['`a, b, c = 1, 2, 3`', '`a = b = c = 0`'],
}
